#include "../include/SessionManager.h"

bool SessionManager::create_session(UserPtr user, size_t id, session_type flag) {
    return false;
}

bool SessionManager::add_user_in_session(size_t id, UserPtr user) {
    return false;
}

int SessionManager::delete_session() {
    return 0;
}

int SessionManager::update_sessions() {
    return 0;
}

int SessionManager::update_with_user_id(size_t user_id, std::string message) {
    return 0;
}

